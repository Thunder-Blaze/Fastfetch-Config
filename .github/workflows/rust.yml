name: Rust

on:
  push:
    branches: [ "critical" ]
  pull_request:
    branches: [ "critical" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Cd
      run: cd tsukiyomi-fetch-rust
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
